# 해커톤 체크리스트

> 인쇄하거나 화면 한쪽에 띄워 두고 쓴다. 각 항목은 **누가 봐도 예/아니오로 답할 수 있게** 썼다.

## 시작 전 (T+0 이전 30분)

전원 각자 확인한다.

- [ ] `git clone` 후 `.\make.ps1 setup` 이 **"환경 준비 완료"** 로 끝났다
- [ ] `.\make.ps1 api` 로 백엔드가 뜨고 `http://127.0.0.1:8000/docs` 가 열린다
- [ ] `.\make.ps1 web` 으로 화면이 뜨고 **위험·위치·행동·시각·119** 다섯 개가 보인다
- [ ] [CLAUDE.md](../CLAUDE.md) 4절의 enum 표와 금칙어 표를 읽었다
- [ ] [DECISIONS.md](./DECISIONS.md) 3절 OPEN 목록을 읽었다
- [ ] 내가 단일 소유자인 파일이 무엇인지 안다 ([RUNBOOK](./HACKATHON_11H_RUNBOOK.md) 1절)
- [ ] `secrets/` 와 원본 데이터가 Git 에서 제외되는 것을 `git check-ignore -v` 로 확인했다

팀 전체로 확인한다.

- [x] 역할이 이름으로 배정됐다 — PM 안려현 · A/E 안윤지(겸임) · B 유진희 · C 박윤후 · D 정예지
- [x] 계약 오너 1명이 정해졌다 — **안윤지**
- [x] 정책(안전정책) 오너 1명이 정해졌다 — **안려현**
- [ ] 저장소 주인이 정해졌고 전원 Write 권한을 받았다

## G0 · T+1:30 — 계약 동결

- [x] `contracts/schema/` 에 4대 계약이 전부 있다 — `risk_assessment` · `action_decision` · `safe_route` · `assess_response`
- [x] `.\make.ps1 contracts` 가 통과한다 (**픽스처 23건** — 유효 12 + 거부 11. 2026-08-17 실사 확인)
- [x] 거부 예제 **11개**가 **전부 거부된다** (통과하면 실패다)
- [x] **4대 계약이 전부 픽스처로 검증된다** — 하나라도 검증하는 픽스처가 없으면 죽은 계약이다 (C-12)
- [x] `ActionDecision.official` 이 `official_0808.json` 을 그대로 받는다 (C-11)
- [x] [DECISIONS.md](./DECISIONS.md) 의 OPEN 항목에 **담당자와 기한**이 채워졌다
- [x] 단일 소유자 표에 실제 이름이 적혔다
- [x] 경로 범위 안 센서 수를 세어 O-09 에 적었다 — **등록 6개 / 재생 시점 실제 5개** (원래 G1 항목이었으나 G0 에서 닫았다)
- [x] `official_0808.json` 의 `verification` 이 `VERIFIED_SOURCE` 다 — **안려현** (O-11 → C-25 완료. 원출처 대조표 30건)
- [x] 데모 시각이 확정됐다 — **2022-08-08 21:40** (O-12 → DECISIONS 3.6). 설계서 초안 20:xx 는 폐기됐다 — **안려현**
- [x] 지역 집계 규칙(TH-04)과 지역 임계가 정해졌다 — **안윤지** (O-01 확정 → C-17. 범위 내 TH-03 초과 비율, 지역 임계 `0.5`. **임계는 `TEAM_AGREED` 이며 검증값이 아니다**)
- [x] `threshold_basis` 표기를 확정했다 — **안윤지** (O-14 확정 → C-19. `val_events@fpr_0.05` 유지. 그 필드가 설명하는 값은 센서 임계라 표기가 정확하다)
- [x] 경계 진동 억제를 정했다 — **안윤지** (O-08 확정 → C-18. 진입 즉시 / 해제 3스텝. **3스텝은 이 사건 하나에서 고른 팀 합의값이다**)
- [x] `AssessResponse.official` 도 `official_0808.json` 을 그대로 받는다 (C-21) — C-11 이 `ActionDecision` 만 고쳐 절반만 닫혀 있었다

**여기서 계약이 안 정해지면 나머지 9시간 30분이 전부 흔들린다. 다른 걸 멈추고 이것부터 끝낸다.**

> `.\make.ps1 check` 가 **실패를 통과로 보고하던 버그가 있었다**(C-16). 고쳤지만,
> 게이트를 믿기 전에 각 단계의 출력이 실제로 화면에 보이는지 한 번은 눈으로 확인한다.

## G1 · T+3:00 — 수직 슬라이스

- [ ] 브라우저에서 `DS-S1` 이 뜨는 것을 **전원이 눈으로 봤다**
- [ ] 5개 행동의 화면 mock 이 존재한다
- [x] ~~**그래프 라우팅 채택 여부를 1회 판단하고 기록했다** (RT-08)~~ — **최종 회의에서 닫았다.** 시도하지 않고 공식 후보 비교로 고정한다 (M-20·M-22 / O-10, 담당 **박윤후**)
- [x] ~~경로 범위 안에 들어오는 센서 수~~ — **G0 에서 닫았다.** [DECISIONS.md](./DECISIONS.md) 3.0.1
- [ ] `.\make.ps1 check` 통과

## G2 · T+9:30~10:30 — 축소 게이트

- [ ] 대표 E2E 1개가 브라우저에서 끝까지 돈다
- [ ] `.\make.ps1 check` 가 통과한다
- [ ] `DS-S1` 외에 최소 1개 시나리오가 더 돈다
- [ ] P1/P2 프로필 값을 **확정했거나, 프로필 기능을 뺐다** (애매하게 두지 않는다)
- [ ] 남은 OPEN 항목을 발표에서 어떻게 말할지 문장으로 정했다
- [ ] **이 시점 이후 새 기능을 만들지 않기로 전원이 합의했다**

## 데모 직전 (T+10:30~11:00)

### 화면 확인

- [ ] 위험 등급이 보인다
- [ ] 현재 위치가 보인다
- [ ] 현재 행동이 보인다
- [ ] 재생 시각이 보인다 — **`clock.label` 그대로**이며 오늘 날짜가 아니다
- [ ] 119 버튼이 보인다 (`EMERGENCY` 에서 커진다)
- [ ] **교육·시연용 면책 문구가 보인다**
- [ ] 경로 제한 문구가 보인다
- [ ] 픽스처 기반이면 그 사실이 화면에 표시된다

### 문구 확인

- [ ] "안전 경로" / "최적 경로" / "검증된 경로" 가 화면에 없다
- [ ] "실시간" 이 화면에 없다
- [ ] "자동 신고" / "자동 위치 전송" 이 화면에 없다
- [ ] "길안내" / "내비게이션" 이 화면에 없다
- [ ] 하수 수위에 m·cm·충만율이 붙어 있지 않다
- [ ] 119 외의 번호가 없다

> `.\make.ps1 test` 의 `test_forbidden_wording.py` 가 이 항목들을 자동으로 본다.
> 다만 **발표 슬라이드와 말은 검사하지 않는다.** 입으로도 쓰지 않는다.

### 발표 내용 확인

- [ ] 선택 모델 성능을 **HistGB 0.648 / 0.029 / 85.3%** 로 말한다
- [ ] Logistic 의 **87.7% 를 선택 모델 성능으로 인용하지 않는다**
- [ ] 하수 고수위를 도로 침수로 단정하지 않는다
- [ ] 1분 간격을 말할 때 "2022년 과거 원본 · 명목상 1분 · 통신두절 누락"을 붙인다
- [ ] 미확정 항목을 정한 척하지 않는다. **남은 OPEN 은 지정 지점 목록 `O-13` 하나다.** 공식정보 실제 값(O-11)·데모 시각(O-12)·`DANGER` 추가 신호(O-15)는 **닫혔으므로 미확정이라고 말하지 않는다** ([DECISIONS.md](./DECISIONS.md) 3.1)
- [ ] **없는 미확정을 말하는 것도 부정확이다.** 경로 실패 정책·프로필 수치·목적지 차단 기준은 최종 회의에서, 관측률 지표(O-16 → C-28)와 안전거점 후보 집합(C-32)은 2026-08-17 에 닫혔다 ([DECISIONS.md](./DECISIONS.md) 2.3.1 · 2.5 · 2.6)
- [ ] **닫힌 항목도 "검증됐다"고 말하지 않는다.** 프로필 수치(1.15·1.5)와 지역 임계(0.5)는 팀 합의값이다 (M-37)
- [ ] 안전거점은 **후보 집합만 7곳으로 닫혔다**고 말한다 — 순위·수용인원 반영·시설 상태 연동은 없고, 7곳도 개방 여부·출입구·지상 안전층은 확인되지 않았다 (C-32 · M-23)
- [ ] TH-04 는 **정했지만** 지역 임계 `0.5` 가 검증값이 아니라 팀 합의값이라는 것과, 범위 안 센서가 5개뿐이라 6단계로만 움직인다는 것을 함께 말한다
- [ ] 공식 대피경로 후보를 안전 보장으로 말하지 않는다

### 리허설

- [ ] `npm run build` + `npm run preview` 로 **production 빌드본**을 띄워 리허설했다 (dev 서버만으로 하지 않는다)
- [ ] 데모를 2회 이상 처음부터 끝까지 돌렸다
- [ ] 발표 시간을 재봤다
- [ ] `.\make.ps1 check` 통과 후 태그 후보를 준비했다

## 장애 복구

### 데모 중

| 증상 | 즉시 할 것 | 관객에게 할 말 |
|---|---|---|
| 지도가 안 뜬다 | 아무것도 안 해도 된다 | "지도 배경은 외부 타일이라 없을 수 있습니다. 위험 판단과 행동 안내는 그대로 보입니다" |
| 백엔드가 죽었다 | `.\make.ps1 api` 창 재실행 | 상태가 없어 즉시 복구된다 |
| 프론트가 하얗다 | 새로고침 → 안 되면 `preview` 빌드본으로 전환 | — |
| 네트워크가 끊겼다 | 그대로 진행 | "예측·판정에 외부 API 를 쓰지 않아서 네트워크 없이도 동작합니다" |
| 화면이 완전히 안 된다 | 픽스처 JSON 을 직접 보여준다 | 계약이 있다는 것 자체가 보여줄 것이다 |

### 코드

```powershell
# main 이 깨졌다 — 되돌린 뒤에 원인을 본다
git log --oneline -10
git revert <커밋해시>

# 내 로컬만 이상하다
git stash
.\make.ps1 check

# 의존성이 꼬였다
Remove-Item -Recurse -Force web\node_modules, .venv
.\make.ps1 setup

# 픽스처를 잘못 고쳤다
git checkout -- contracts/fixtures/
.\make.ps1 fixtures
```

**`git reset --hard` 를 쓰지 않는다.** 남의 작업이 사라진다.

### 사고

| 상황 | 대응 |
|---|---|
| API 키를 커밋했다 (push 전) | `git reset --soft HEAD~1` 후 다시 커밋 |
| API 키를 push 했다 | **먼저 서울시 포털에서 키를 재발급한다.** 이력 정리는 그 다음 |
| 대용량 파일을 push 했다 | `git rm --cached <파일>` + `.gitignore` 추가 |
