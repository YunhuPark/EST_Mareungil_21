# 데이터·모델 파이프라인 의존성 (scripts/).
#
# 앱 개발자는 설치할 필요가 없다. AI·데이터 담당만 설치한다.
#
#     .\make.ps1 install-model
#
# 아래 버전은 이 저장소의 모델 결과(재현율 0.648 / 오경보 0.029 / 사전감지 85.3%)를
# 만들어낸 환경 기준이다. 바꾸면 보고한 수치를 다시 검증해야 한다.

pandas==2.3.3
numpy==2.3.5
scikit-learn==1.6.1
pyarrow==21.0.0
