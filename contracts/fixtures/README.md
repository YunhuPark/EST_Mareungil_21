# 계약 픽스처

계약(모듈 간 주고받는 JSON)의 **값이 채워진 실제 예시 파일**이다.
스키마 문서만 있으면 네 명이 각자 다르게 해석하지만, 값이 박힌 파일이 있으면
해석의 여지가 없다. 목 서버가 이 파일을 그대로 응답하면 백엔드 없이도
UI를 끝까지 만들 수 있다.

## 지금 있는 것

`RiskAssessment` (① 예측 → ② 의사결정) 만 있다. 나머지 3종은 계약 회의에서 정한다.

| 파일 | 시점 | 상황 |
|---|---|---|
| `risk_S1_calm.json` | 2022-08-08 11:00 | 평상 — 무강우, 고수위 0개, 경보 0개 |
| `risk_S2_rising.json` | 2022-08-08 12:10 | **강우 상승 — 고수위 0개인데 경보 13개** |
| `risk_S3_peak.json` | 2022-08-08 21:40 | 사건 정점 — 고수위 29개, 경보 30개 |
| `risk_S4_recovery.json` | 2022-08-09 09:00 | 회복 — 고수위 14개로 감소 |
| `risk_E1_no_data.json` | — | 예외. 손으로 작성한 유일한 파일 |

`_index.csv` 에 네 시점의 요약 수치가 있다.

## S2 가 데모의 핵심이다

12:10 시점에 **물이 높은 센서는 하나도 없다.** 그런데 모델은 13개 센서에 경보를 켠다.

30분 뒤 12:40에 **13개 중 12개가 실제로 고수위가 됐다.** 같은 30분 동안
전체 고수위 센서는 0개에서 29개로 늘었다.

"지금 물이 높으면 나중에도 높다"고만 답하는 기준선은 12:10에 경보를 하나도
못 켠다. 모델이 만든 가치가 이 한 장면에 전부 들어 있다.

발표 동선은 **S1 → S2 → S3** 으로 시간을 흘려보내는 것이 가장 강하다.

## 값은 지어내지 않았다

`_scenario` 로 시작하는 필드를 뺀 나머지는 전부 **실제 모델 출력**이다.
`scripts/build_demo_fixtures.py` 로 재생성된다.

이 사건(2022-08-08)은 **학습에서 완전히 제외된 test 사건**이다. 모델이 한 번도
본 적 없는 규모의 호우에 대해 내놓은 값이라는 뜻이고, 발표에서 그렇게 말해도 된다.

## 계약 회의에서 정해야 할 것

- **`level` 등급이 없다.** HIGH/MEDIUM/LOW 경계는 정책이므로 ①이 확률만 주고
  ②가 매기는 쪽으로 제안한다. 반대 의견이 있으면 회의에서 확정.
- **`drivers[].contribution` 이 전부 null.** SHAP 미설치라 기여도를 지어내지 않았다.
  피처 값만 실제로 실려 있다. 해커톤 작업 항목.
- **센서를 33개 전부 보낼지** 상위 N개만 보낼지.
- `location.lat/lon` 이 **공식 좌표가 아니다.** `quality` 를 반드시 함께 보고
  판단할 것. `LANDMARK_MATCH_MANUAL_REVIEW` · `ROAD_NAME_ONLY_APPROX` 는
  도로 차단에 쓰면 안 되고 시각화까지만.
- `predicted_level_unit` 이 `UNCONFIRMED` 다. **화면에 m·cm 를 붙이지 말 것.**

## 재생성

```powershell
python scripts\build_demo_fixtures.py
```
